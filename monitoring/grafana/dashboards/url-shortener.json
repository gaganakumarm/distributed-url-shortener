{
  "annotations": {"list": []},
  "editable": true,
  "panels": [
    {
      "type": "timeseries",
      "title": "Requests per second",
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "targets": [{"expr": "sum by (instance) (rate(url_shortener_http_requests_total[5m]))", "legendFormat": "{{instance}}"}],
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0}
    },
    {
      "type": "timeseries",
      "title": "p95 latency",
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "targets": [{"expr": "histogram_quantile(0.95, sum by (le, route) (rate(url_shortener_http_request_duration_seconds_bucket[5m])))", "legendFormat": "{{route}}"}],
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0}
    },
    {
      "type": "timeseries",
      "title": "HTTP errors",
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "targets": [{"expr": "sum by (status) (rate(url_shortener_http_requests_total{status=~\"4..|5..\"}[5m]))", "legendFormat": "HTTP {{status}}"}],
      "gridPos": {"h": 8, "w": 24, "x": 0, "y": 8}
    }
  ],
  "schemaVersion": 41,
  "tags": ["url-shortener"],
  "templating": {"list": []},
  "time": {"from": "now-1h", "to": "now"},
  "title": "Distributed URL Shortener",
  "uid": "distributed-url-shortener",
  "version": 1
}
